import React from 'react';

const State = () => {
	return (
		<section>
			<h4 className='font-bold text-3xl'>State</h4>
			<span className='text-muted-foreground'>
				State is a fundamental concept in React that allows components to dynamically update and render based on data that changes over
				time.
			</span>
		</section>
	);
};

export default State;
